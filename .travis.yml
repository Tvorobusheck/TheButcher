language:
  - cpp
compiler:
  - clang
  - gcc

before_install:
  - pip install --user cpp-coveralls

script:
  - cd gtest/Test_TheButcher
  - qmake
  - make
  - ./Test_TheButcher

after_success:
  - coveralls --root . -E ".*external.*" -E ".*tests.*"

notifications:
email: false